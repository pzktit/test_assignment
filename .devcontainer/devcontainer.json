{
  "name": "crypto-labs",

  "build": {
    "dockerfile": "Dockerfile"
  },

  "features": {
    "ghcr.io/devcontainers/features/common-utils:2": {}
  },

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "ms-toolsai.jupyter",
        "ms-toolsai.jupyter-keymap",
        "ms-toolsai.jupyter-renderers"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/workspaces/${containerWorkspaceFolderBasename}/.venv/bin/python"
      }
    }
  },

  "remoteEnv": {
    "PATH": "/home/vscode/.local/bin:${PATH}"
  },

  "postCreateCommand": "bash -lc \"curl -LsSf https://astral.sh/uv/install.sh | sh && ~/.local/bin/uv venv && source .venv/bin/activate && ~/.local/bin/uv pip install -r requirements.txt && python -m ipykernel install --user --name crypto-env --display-name 'Python (crypto)'\"",

  "forwardPorts": [8888, 5000, 8000]
}
